/*global pmw, $*/pmw.Controllers = pmw.Controllers || {};(function(global) {    'use strict';    pmw.Controllers.ChooseLocationController = pmw.Controllers.AbstractController.extend({        pageHeadline: "Pimp My Wall",        _initViews: function() {            backRoute = "/";            // Create the ContentView with the controller (this) as scope            if (!this.contentView) {                this.contentView = pmw.Views.ChooseLocationView.create(this, null, true);            }            // Create the HeaderView with the controller (this) as scope            if (!this.headerView) {                this.headerView = M.ToolbarView.extend({                    grid: 'col-md-12',                    value: 'Pimp My Wall'                }).create();            }            this._applyViews();            var that = this;        },        gotoPage: function(page) {            pmw.navigate({                route: '/' + page            });        }    });})(this);